import com.sun.xml.internal.ws.api.model.wsdl.WSDLOutput;
import org.opencv.core.*;
import org.opencv.imgproc.Imgproc;

import java.util.Map;

public class Main {
    static {
        System.loadLibrary(Core.NATIVE_LIBRARY_NAME);
    }

    public static void main(String[] args) {
/*        //создание точки с переданными координатами
        //без передачи координат создается точка (0;0)
        Point p1 = new Point(10.0, 20.0);
        Point p2 = new Point();

        // set - задать новое значение передается массив double[] с двумя значениями внутри
        // так же возможно изменение значения в таком виде:
        // p1.x = 2
        System.out.println(p1.x + " " + p1.y);  // 10.0 20.0
        p1.set(new double[]{15.0, 25.0});      // 15.0 25.0
        System.out.println(p1.x + " " + p1.y);
        p1.x = 2;
        p1.y = 3;
        System.out.println(p1.x + " " + p1.y);  // 2.0 3.0

        Mat img = new Mat(300, 300, CvType.CV_8UC3, CvUtils.COLOR_WHITE);
        */


    }
}
